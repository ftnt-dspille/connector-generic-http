name: Release

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Get version from tag
        id: version
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Build connector tarball
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          tar -czf "connector-generic-http-${VERSION}.tgz" generic-http/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: "Generic HTTP Connector v${{ steps.version.outputs.version }}"
          body_path: generic-http/release_notes.md
          files: "connector-generic-http-${{ steps.version.outputs.version }}.tgz"
